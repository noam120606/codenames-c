#ifndef VERSION_H
#define VERSION_H

#include "../lib/sdl.h"

/**
 * Charge la version du serveur dans la structure AppContext.
 * @param context Pointeur vers la structure AppContext.
 */
void load_version(AppContext* context);

#endif // VERSION_H